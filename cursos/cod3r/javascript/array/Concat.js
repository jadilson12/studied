// Junta mais de array

const pessoas = ['Ana', 'Geraldo']
const filhos = ['Gilson', 'Jerson']
const todos = pessoas.concat(filhos)
console.log(todos, filhos)

const valores = ['a', 'b']
console.log(valores.concat([1, 3, [1, 3]]))